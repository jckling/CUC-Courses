
var a, b, c, max;

procedure p;
begin
    read(a);
    read(b);
    read(c);
    if a > b
    then max := a;
    
    if b > a
    then max := b;
    
    if max > c
    then max := max;

    if c > max
    then max := c;

    write(max);
end;

call p.
